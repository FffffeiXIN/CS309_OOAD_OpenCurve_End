<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sustech.service_education.mapper.VideoMapper">

    <select id="getURLByKey" resultType="java.lang.String">
        select url from video where course_id = #{course_id} and session = #{session};
    </select>
    
    <insert id="insertVideo">
        insert into video (course_id, session, url) values (#{course_id}, #{session}, #{url});
    </insert>
</mapper>
